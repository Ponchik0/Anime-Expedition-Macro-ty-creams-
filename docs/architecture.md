# Архитектурная карта движка

Разбор `Cweamy/Anime-Expeditions-Creams-Macro` v0.14.0 (коммит `f7bdf4d`).
Python 3.14, ~16 000 строк в `core/`, ~11 000 строк в `ui/`.

---

## 1. Общая схема

```
main.py  (3823 строки)
   │  pywebview: создаёт окно, встраивает в него окно Roblox,
   │  поднимает класс Api — это мост JS ↔ Python
   │
   ├── ui/          фронтенд: HTML + CSS + JS в WebView2
   │                вызывает Python как pywebview.api.<метод>()
   │
   └── core/        вся логика: зрение, ввод, окно, прогон матча
```

Важно понимать: **это не десктопное приложение на виджетах, а веб-страница
в окне.** Вся вёрстка — обычные HTML/CSS/JS. Для нашего редизайна это скорее
плюс: переверстать `ui/` можно как обычный сайт, не трогая Python.

### Как окно игры оказывается внутри приложения

`core/dock.py` — «докинг». Два режима:

- **Windows (обычный):** окно Roblox делается дочерним окном нашего окна
  (`SetParent`). Игра физически живёт внутри интерфейса.
- **Cutout-режим:** Roblox остаётся отдельным окном, приклеенным позади дырки
  в нашем окне. Нужен, когда обычный захват экрана возвращает чёрные кадры.
- **macOS:** встроить чужое окно нельзя в принципе — панель и игра просто
  стоят рядом.

Размер окна игры фиксирован: **1152 × 756** (`config.FIXED_WIN_W/H`). Из этого
следует важное: **все координаты в движке — в этой системе**, и задача
«пересчитать под любой размер окна», которая занимала столько сил в AHK-версии,
здесь решена иначе — окно просто всегда одного размера.

---

## 2. `core/` — по модулям

### Ввод (мышь и клавиатура)

| Модуль | Что делает |
|---|---|
| `mouse.py`, `keyboard.py` | единый кросс-платформенный API: `click`, `move_to`, `tap`, `combo` |
| `_input_win.py`, `_input_mac.py` | платформенные реализации одного и того же набора примитивов |
| `_sendinput.py` | сырой Win32 `SendInput` |
| `keys.py` | таблица виртуальных кодов клавиш |
| `pacing.py` | глобальный «замедлитель»: одна общая задержка во всех точках ввода |

Ввод идёт через `SendInput`, а не `SetCursorPos` — по той же причине, что и
`SendMode Input` в AHK: игры слушают настоящий поток ввода, а простую запись
позиции курсора игнорируют.

`pacing.py` — красивое решение той же проблемы, что в AHK решалась россыпью
`RandSleep()` по коду: одна ручка «Macro Speed» добавляет задержку в общих
точках (`Mouse.click`, `Keyboard.tap`), а не в пятидесяти местах.

### Окно

| Модуль | Что делает |
|---|---|
| `window.py` | диспетчер: выбирает реализацию по ОС |
| `window_win.py` (724) | поиск окна Roblox, геометрия, DPI, докинг-примитивы |
| `window_mac.py` (507) | то же через Quartz/Accessibility |
| `dock.py` | встраивание окна игры, сторож переподключения |

### Зрение — **`core/vision.py` (1134 строки), ключевой модуль**

Отвечает на вопрос «где на экране вот эта кнопка».

- Эталоны лежат в `Assets/ui/<имя>/` — **папка на каждое искомое имя**, внутри
  один или несколько PNG-вариантов одной и той же кнопки. Все варианты
  пробуются как взаимозаменяемые.
- Сравнение **в оттенках серого**, не в цвете. Причина ровно та же, что мы
  выстрадали в AHK: текст в этой игре на градиентной заливке, цвет плывёт по
  букве, а яркость кнопки зависит от экрана.
- Матчинг через OpenCV `matchTemplate` с порогом; порог настраивается **на
  каждое изображение отдельно** (Image Manager → sensitivity).
- Несколько источников кадра: `mss` (снимок экрана), `PrintWindow` (свой
  бэкбуфер окна — не мигает), `wgc_capture.py` (Windows.Graphics.Capture —
  для случаев, когда первые два дают чёрный кадр).

Функции, которыми пользуется весь остальной код:
`find_image(hwnd, name, threshold)` и `wait_for_image(hwnd, name, timeout)`.

### OCR

| Модуль | Что делает |
|---|---|
| `ocr_windows.py` | **основной** — встроенный в Windows `Windows.Media.Ocr`, ничего ставить не надо, в ~16 раз быстрее Tesseract |
| `ocr.py` | общая обвязка + Tesseract как запасной вариант |
| `wave.py` | читает счётчик волны «0 / 15 wave» |
| `rewards.py` | читает строку наград на экране победы |
| `game_stats.py` | читает сетку статистики матча |
| `stage_data.py` | справочник ожидаемых наград с вики |

### Прогон матча — `core/runner.py` (3650) + миксины

`MacroRunner` собран из миксинов, механически разнесённых по файлам:

| Файл | Зона ответственности |
|---|---|
| `runner.py` | главный цикл: лобби → выбор карты и стадии → Pre Start → Start → бой → Victory/Defeat |
| `runner_blocks.py` (1439) | **исполнение блоков** — установка юнитов, апгрейды, продажа, ожидания, ходьба |
| `runner_constants.py` (600) | все константы и цветовые предикаты в одном месте |
| `runner_expedition.py` | режим Expedition: волны, чекпоинты, эвакуация |
| `runner_challenge.py` | режим Challenge: окна доступности, дневные лимиты |
| `runner_bounty.py` | доска заданий |
| `runner_crafting.py` | крафт между матчами |

#### Как определяется, что матч кончился

Основной признак — баннер `victory` / `defeat`. Но он один был и **единственным**,
а шаблоны сняты на настройках автора движка: не совпал ни один — и другого
признака нет, макрос опрашивал экран полные 30 минут
(`MATCH_RESULT_TIMEOUT`), стоя на уже законченном матче, потом объявлял задачу
сломанной. Со стороны это «игра кончилась, а переигровка не работает».

Второй, независимый признак — **кнопка `repeat_stage`**
(`_match_ended_without_a_banner`). Она есть только на экране результата: в бою
на её месте живой Leave Stage, а это отдельное имя поиска
(`Assets/ui/leave_stage/*_live*`). Увидели кнопку → матч кончился:

1. баннер перепроверяется мягче (`MATCH_RESULT_RELAXED_THRESHOLD = 0.80` против
   обычных 0.90) — панель на экране, значит баннер там тоже есть;
2. не помогло → результат `RESULT_UNKNOWN` (`"ended"`). Фарм **продолжается**
   обычным путём (Repeat Stage / выход в лобби), но матч не идёт ни в
   статистику, ни в уведомление о результате — врать «победа» или «поражение»
   нельзя, по этим числам считаются винрейт и предохранитель серии поражений.
   Вместо этого уходит событие «Итог матча не распознан» + скриншот, чтобы
   человек добавил свою вырезку через Менеджер картинок.

Третий признак — **`nav_start_game` снова на экране**
(`_round_ended_back_at_start_game`). Так устроен **raid**: экрана результата с
Repeat Stage там нет вообще, раунд просто заканчивается и висит кнопка старта.
Результат — `RESULT_ROUND_ENDED`: `_handle_match_result` не вызывается (нечего
обрабатывать), в лобби не выходим (мы и так в этапе), а следующий повтор задачи
запускается прямо здесь — он заново отработает Pre Start (поле между раундами
очищается) и сам нажмёт Start Game. Порог `MATCH_END_START_GAME_MIN_S = 25 с`
обязателен: кнопка видна и в первые секунды боя, и без него «раунд кончился»
срабатывало бы сразу после старта.

Нажимать её на месте, как это делает Expedition, **нельзя**: поле между
раундами очищается, и забег, начатый одним кликом, пойдёт без юнитов.

Уведомление при этом всё равно уходит (`_report_round_ended`) — та же карточка,
что после expedition, но с вердиктом «не распознан», без картинки состояния
(она рисуется в цветах победы/поражения) и с обязательным скриншотом, по
которому и делается недостающая вырезка. В счётчики такой раунд не пишется:
`_finish_match_result_background(record=False)`.

Кнопка опрашивается раз в `MATCH_END_CHECK_EVERY = 5` тиков: поиск шаблона по
всему окну не бесплатный, а баннеры и так ищутся дважды в секунду.

#### Два предохранителя, которые обязаны различать «проиграл» и «сломался»

Оба срабатывали на ровном месте именно в Expedition, где проиграть — норма.

**Серия поражений** (`MAX_CONSECUTIVE_LOSSES_SAME_MAP = 3`). Три поражения
подряд на одной карте считались признаком поломки: макрос закрывал Roblox через
`taskkill`, переоткрывал по диплинку и присылал красное уведомление. В
Expedition умереть до точки извлечения — обычный забег, и на задаче с
`repeat=999` это шло по кругу. Теперь такое поражение в серию не идёт —
`_is_early_expedition_loss`, признак «не дошли до извлечения» это
`_expedition_extract_count == 0`. Дошли до чекпойнта и всё равно проиграли —
считается как раньше.

**Сторож бездействия** (`STALL_TIMEOUT_MIN = 25`) считает прогрессом изменение
строки текущего действия. Во время боя её не менял никто (в Expedition тем
более — там нет ни Battle-блоков по умолчанию, ни чтения номера волны), а порог
сторожа **меньше** таймаута матча (`MATCH_RESULT_TIMEOUT = 30 мин`), то есть
достаточно длинный забег гарантированно останавливал весь прогон с диагнозом
«завис». Теперь бой сам отбивает такт раз в минуту (`_pulse_battle_status`:
«Бой: 7 мин, чекпойнт 1/2»), и замершая строка снова означает именно то, что
должна.

### Система блоков — как здесь устроена «программа матча»

Это главное архитектурное отличие от моего AHK-макроса.

В AHK было: список юнитов + у каждого точка и число апгрейдов. Жёсткая схема.

Здесь: **Macro Operation** — сценарий из блоков, который пользователь собирает
в UI (экран Creation). Блоки бывают двух фаз:

- **Pre Start** — до нажатия «Start Game» (настройка камеры, лоадаут, ходьба,
  предварительная расстановка);
- **Battle** — во время боя (доставить юнита, апгрейдить, продать, ждать волну).

Типы блоков: `place_unit`, `upgrade`, `sell`, `wait`, `wait_for_wave`, `walk`,
`setting`, `detect`, и т. д.

`core/detect.py` — единственный ветвящийся блок: ищет картинку и выполняет одну
из двух вложенных веток (`then` / `else`). Реализован хитро: вложенность
**разворачивается в плоский список** с двумя служебными операциями перехода,
чтобы движок исполнения остался простым циклом с одним индексом.

### Установка юнита — `_run_place_unit_block` (`runner_blocks.py:1039`)

Как это работает сейчас:

1. Нажать `Z` (сброс текущего выделения), затем **хоткей юнита** — это прямой
   аналог нашей «клавиши слота 1-6».
2. Найти валидную клетку: игра подсвечивает место установки **белым**. В
   квадрате 38×38 вокруг сохранённой точки ищется подсветка, в три уровня
   (`_scan_place_search_box`):
   - сама точка уже белая → смещения нет;
   - иначе — ближайшее достаточно **крупное пятно**, клик в его центр;
   - крупного пятна нет → ближайший белый пиксель, как было раньше.

   Уровни важны: одиночный белый пиксель — это и белый текст, и частица, и
   кромка платформы, поэтому раньше клик уходил на 25 px мимо клетки и юнит
   не ставился. Если не нашлось ничего — расширяющийся поиск по кольцам
   радиусом 24 / 48 / 72 px, бюджет 8 секунд. Есть галка «игнорировать
   подсветку» — тогда клик вслепую по координате.
3. Клик **с проверкой** (`_click_place_spot`): пока юнит в руке, под курсором
   горит подсветка. Погасла → клик прошёл; осталась → не прошёл, и клик
   повторяется до 3 раз. Хоткей при этом заново НЕ нажимается, поэтому дубль
   поставить невозможно; в цепочке quick-place повтор отключён (там юнит
   остаётся выбранным) и промах только пишется в журнал.
4. Проверка `max_placement_reached` — не упёрлись ли в лимит юнитов.
5. **Верификация:** ищется картинка `unit_exist` (панель информации о юните).
   Если не видно — кликнуть по юниту и проверить снова, до 3 попыток по 2 с.
   В Pre Start этот шаг пропускается ради скорости — там достаточно проверки
   из пункта 3.
6. Опция «Keep Placing» — повторить весь цикл целиком до 5 раз.

**Догоняющая расстановка** (`_retry_pending_placements`). Клетка нашлась, клик
нажат, юнит не встал — это почти всегда «не хватило стартовых денег», а не
сломанные координаты. Такой блок не выбрасывается, а запоминается и
доставляется **уже в бою**, когда деньги накопились: по одному юниту за опрос
цикла боя, до 4 подходов с паузой 12 с, в пределах первых 5 минут матча.
Догоняющая попытка заново жмёт хоткей и проходит ту же проверку подсветки,
поэтому дубль поставить не может: если юнит всё-таки стоял, клетка под ним уже
не белая.

**Итог расстановки** в журнале — дважды за матч: после Pre Start и после боя
(`_log_placement_tally`, второй раз показывает только новое). Считаются три
вещи: встало, не встало, **пропущено** (у блока нет хоткея или точки — шесть
таких блоков это то же пустое поле, что и шесть непоставленных юнитов).
Нулевая расстановка в Pre Start отмечается отдельно и громко — это заведомо
проигранный забег с пустым полем.

Оптимизация «quick place»: если подряд идут блоки установки **одного и того же**
юнита, между ними держится Shift — юнит остаётся выбранным, и каждая следующая
установка пропускает нажатие хоткея.

Константы (`runner_constants.py:385-415`):

```
PLACE_SEARCH_BOX_SIZE = 38        поиск белой клетки вокруг точки
PLACE_SPIRAL_RADII = (24, 48, 72) кольца расширяющегося поиска
PLACE_SPIRAL_TIMEOUT = 8.0        бюджет на весь поиск
PLACE_HOTKEY_SETTLE = 0.35        подсветка клетки должна успеть отрисоваться
PLACE_UNIT_CLICK_SETTLE = 0.25
PLACE_UNIT_VERIFY_TIMEOUT = 2.0
PLACE_UNIT_VERIFY_ATTEMPTS = 3
PLACE_RETRY_UNTIL_PLACED_ATTEMPTS = 5
PLACE_HIGHLIGHT_MIN_PIXELS = 14   пятно меньше — блик или частица, не клетка
PLACE_HIGHLIGHT_MIN_SIDE = 4      и в ширину, и в высоту: штрих буквы не пройдёт
PLACE_CONFIRM_ATTEMPTS = 3        перещёлкивания, если подсветка не погасла
PLACE_CONFIRM_SETTLE = 0.35       дать игре дорисовать результат клика
PLACE_CONFIRM_PROBE_SIZE = 5      пятачок под курсором для проверки клика
PLACE_CONFIRM_PROBE_RATIO = 0.6   какая его доля должна быть белой
PLACE_PENDING_FIRST_WAIT_S = 8.0  догонять не раньше: пусть волна начнётся
PLACE_PENDING_RETRY_S = 12.0      пауза между подходами к одному юниту
PLACE_PENDING_MAX_TRIES = 4       подходов на юнита, потом сдаёмся
PLACE_PENDING_DEADLINE_S = 300.0  позже этого догонять бессмысленно
```

### Прочее

| Модуль | Что делает |
|---|---|
| `settings.py`, `config.py`, `jsonstore.py` | настройки в `settings.json`, атомарная запись |
| `paths.py` | запись и воспроизведение пути ходьбы (WASD опросом состояния клавиш) |
| `webhook.py`, `status_card.py`, `share.py` | Discord: отправка, отрисовка карточки результата через OpenCV |
| `logger.py` | журнал |
| `diagnostics.py` | структурированные отчёты об отказах |
| `templates.py`, `task_presets.py` | сохранённые сценарии и очереди задач |
| `maps.py`, `stage_select.py` | каталог карт, выбор стадии по картинке названия |
| `updater.py` | автообновление — **нами отключено** |
| `camera.py` | постановка камеры в стандартный вид перед матчем |
| `bounty.py` | разбор доски заданий по цвету текста |
| `mss_manager.py` | пул объектов захвата экрана по потокам |

---

## 3. `ui/` — фронтенд

| Файл | Строк | Что это |
|---|---|---|
| `app.js` | 6445 | вся логика интерфейса |
| `index.html` | 2220 | разметка всех экранов |
| `style.css` | 2033 | оформление |
| `tailwind.js` | 83 | конфиг Tailwind |
| `log_view.js`, `logs_window.html` | 294 | отдельное окно логов |
| `wave_monitor.html` | 83 | отдельное окно монитора волн |

### Экраны

Все экраны лежат в одном `index.html` как `<div id="screen-*">`, переключение —
показать/спрятать:

| id | Что на нём |
|---|---|
| `screen-dashboard` | статус, счётчики побед/поражений, кнопки Start/Stop/Pause, лента логов |
| `screen-task` | очередь задач: что за чем запускать |
| `screen-creation` | **редактор Macro Operation** — сборка сценария из блоков |
| `screen-resource` | Challenge / Bounty / Crafting |
| `screen-settings` | всё остальное: хоткеи, отладка, вебхук, темы |

#### Выбор точки установки (`#pu-modal`) и снимок по умолчанию

Одно окно на три вызова: кнопка «Set» у блока Place Unit, та же у блока Click и
Настройки → Отладка → Macro Coordinates (`openCoordPicker`). Внутри — вкладки
категорий из `Assets/map/<Категория>/`, сетка карт и холст, на котором клик
превращается в координату.

Готовых карт в поставке нет намеренно: клик по чужому кадру промахивается,
ракурс камеры у каждого свой (`Assets/map/README.txt`). Точный путь один —
«Снимок из игры» (`get_roblox_snapshot`), и раньше его приходилось повторять на
**каждую** точку.

**Снимок по умолчанию для режима.** Кадр сохраняется один раз и дальше
подставляется сам, как только окно открыто для сценария этого режима:

- хранится обычным PNG — `Assets/map/<Режим>/Snapshot.png` (`core/maps.py`:
  `MODE_CATEGORIES`, белый список — имя режима попадает в путь на диске), то
  есть виден и как обычная карта, и удаляется стиранием файла;
- ключ — **режим, а не карта**: точка задаётся в экранных координатах и зависит
  от ракурса камеры, а камеру макрос выставляет одинаково для всего режима;
- сохраняются те самые байты, что человек видел на холсте (`main.py` помнит
  последний снимок в `_map_snapshot_png`), а не новый снимок экрана — к моменту
  нажатия игра нарисует уже другую волну;
- режим сценария выводится из очереди задач (`resolvePlaceUnitMode`): сценарий
  сам по себе к режиму не привязан, зато задачи ссылаются на него по имени.
  Неоднозначно — берётся прошлый выбор, и его можно поменять селектором;
- в режиме выбора координат из Настроек эти органы управления скрыты — там
  режима нет.

### Оформление сейчас

CSS-переменные в `:root`, тем — около десятка (`data-theme-base` /
`data-theme-accent`).

```
--bg-deep: #11131c   --bg-card: #171a26   --bg-surface: #1c2030
--border:  #2a2e42   --text:    #e2e4ef
--brand: #7c9dff   --teal: #45c9b5   --amber: #e3b158
--lilac: #b58ce0   --rose: #d9667a   --sky:  #5aa9e6
```

То есть ровно то, от чего мы уходим: **сине-фиолетовая база**, шесть акцентных
цветов вместо одного, есть тема со стеклом. По подсчёту: **17 градиентов** и
**7 мест с `backdrop-filter` / `blur()`** в `style.css`.

Хорошая новость для редизайна: цвета уже вынесены в переменные, а разметка
размечена классами `rp-panel`. Значит переверстать можно системно, а не
переписывая 2220 строк HTML вручную.

---

## 4. Что это значит для нас

**Сильные стороны движка, которые нам достались даром:**

- готовый докинг окна игры и фиксированный размер 1152×756 — снимает всю боль
  с пересчётом координат, на которую в AHK ушла половина кода;
- зрелый слой зрения с тремя способами захвата кадра и порогом на каждую
  картинку;
- быстрый встроенный OCR Windows;
- система блоков — гибче, чем моя жёсткая схема «юнит + точка + апгрейды»;
- поиск валидной клетки по белой подсветке — этого в AHK не было вообще,
  и это честнее, чем слепой клик по координате;
- готовые режимы Story / Raid / Challenge / Expedition / Bounty / Crafting;
- Discord-вебхук с отрисованной карточкой результата.

**Слабые места, которые придётся закрывать нам (см. `from_ahk.md`):**

- юнит выбирается **только хоткеем**, поиска по иконке нет — если хотбар
  перемешается, поставится не тот юнит и никто этого не заметит;
- верификация установки **одинарная** (`unit_exist`), нет проверок по деньгам
  и по изменению карты, нет понятия WRONG_UNIT;
- нет аварийного стопа по числу подряд идущих провалов установки (есть только
  по трём поражениям подряд на одной карте);
- нет папки `units/` с автосканом иконок;
- в логах нет фильтра, поиска и экспорта.
